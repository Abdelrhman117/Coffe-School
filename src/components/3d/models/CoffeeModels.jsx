// Step 3 will implement: Tamper, Cezve, Bottle, Bag, Cup with Drei <Decal> branding.
// Each model receives `active` + `scrollProgress` props driven by GSAP ScrollTrigger.

export function TamperModel({ active, scrollProgress }) { return null }
export function CezveModel({ active, scrollProgress }) { return null }
export function BottleModel({ active, scrollProgress }) { return null }
export function BagModel({ active, scrollProgress }) { return null }
export function CupModel({ active, scrollProgress }) { return null }
